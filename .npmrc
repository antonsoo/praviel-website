# pnpm configuration for Cloudflare Workers compatibility
# Required to fix "Dynamic require" errors with @opennextjs/cloudflare
# This flattens node_modules structure instead of using symlinks
shamefully-hoist=true
node-linker=hoisted
