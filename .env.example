# ============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================================================
# Copy this file to .env or .env.local and fill in your actual values
# .env = Production variables (gitignored)
# .env.local = Local development variables (gitignored)
# ============================================================================

# ----------------------------------------------------------------------------
# Public Variables (exposed to browser, safe to commit to wrangler.jsonc)
# ----------------------------------------------------------------------------
# Stack Auth - Get from https://stack-auth.com/dashboard
NEXT_PUBLIC_STACK_PROJECT_ID=5c4f0ecc-f06f-4947-a471-36501ce65909
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=pck_your_publishable_key_here

# Application URLs
NEXT_PUBLIC_APP_ORIGIN=https://praviel.com                # Production: https://praviel.com | Local dev: http://localhost:3000
NEXT_PUBLIC_API_BASE=https://api.praviel.com              # Production: https://api.praviel.com | Local dev: http://localhost:8000
NEXT_PUBLIC_CF_ANALYTICS_TOKEN=                           # Cloudflare Web Analytics token (optional)
NEXT_PUBLIC_ANALYTICS_PROVIDER=cloudflare                 # cloudflare | vercel
NEXT_PUBLIC_ANALYTICS_SAMPLE_RATE=1                       # 0-1 float controlling event sampling

# ----------------------------------------------------------------------------
# Stack Auth (Server-side secret)
# ----------------------------------------------------------------------------
STACK_SECRET_SERVER_KEY=ssk_your_secret_key_here

# ----------------------------------------------------------------------------
# Database (Neon Postgres)
# ----------------------------------------------------------------------------
# Get connection string from https://console.neon.tech
# Production branch: postgresql://user:password@ep-xxx-pooler.region.azure.neon.tech/dbname?sslmode=require
# Development branch: postgresql://user:password@ep-yyy-pooler.region.azure.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://username:password@ep-xxx-pooler.eastus2.azure.neon.tech/neondb?sslmode=require

# ----------------------------------------------------------------------------
# Redis (Railway)
# ----------------------------------------------------------------------------
# Get from Railway Redis service dashboard
# Public URL (for external access, e.g., from Cloudflare Workers or local dev):
REDIS_URL=redis://default:password@proxy.rlwy.net:12345

# Internal URL (for Railway-to-Railway communication, if needed):
REDIS_INTERNAL_URL=redis://default:password@redis.railway.internal:6379

# ----------------------------------------------------------------------------
# Email (Resend)
# ----------------------------------------------------------------------------
# Get API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM_ADDRESS=noreply@praviel.com
EMAIL_FROM_NAME=PRAVIEL
EMAIL_PROVIDER=resend

# ----------------------------------------------------------------------------
# AI Model API Keys (Server-side only - OPTIONAL)
# ----------------------------------------------------------------------------
# OpenAI - Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your_openai_key_here

# Anthropic - Get from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your_anthropic_key_here

# Google AI - Get from https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# ----------------------------------------------------------------------------
# Observability (Sentry)
# ----------------------------------------------------------------------------
SENTRY_DSN=https://public_key@o000000.ingest.sentry.io/000000
SENTRY_AUTH_TOKEN=sntrys_YOUR_TOKEN_HERE
SENTRY_ORG=praviel
SENTRY_PROJECT=praviel-site
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.3
SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.05
SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=1
NEXT_PUBLIC_SENTRY_DSN=https://public_key@o000000.ingest.sentry.io/000000

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
#
# For Cloudflare Workers deployment (via wrangler):
# 1. Public variables go in wrangler.jsonc "vars" section (already configured)
# 2. Secrets must be set via command line:
#    pnpm wrangler secret put STACK_SECRET_SERVER_KEY
#    pnpm wrangler secret put DATABASE_URL
#    pnpm wrangler secret put REDIS_URL
#    pnpm wrangler secret put RESEND_API_KEY
#    pnpm wrangler secret put OPENAI_API_KEY          # optional
#    pnpm wrangler secret put ANTHROPIC_API_KEY       # optional
#    pnpm wrangler secret put GOOGLE_API_KEY          # optional
#
# For local development:
# - Use .env.local for `pnpm dev` (Next.js dev server)
# - Use .dev.vars for `pnpm preview` (Wrangler local preview)
# ============================================================================
